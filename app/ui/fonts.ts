import { Albert_Sans, Inter, Lusitana } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const albert = Albert_Sans({subsets: ['latin']});
export const lusitana = Lusitana({subsets: ['latin'], weight: ['700']});